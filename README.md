# journal-rag
An agent that retrieves context from a journal to answer questions like the journal's author

To use:
1) Install and run Ollama (https://ollama.com). Download a model such as llama3.2.
2) Navigate to your working directory in terminal and use pip to install the packages in requirements.txt.
3) Open and run journal_rag.ipynb
